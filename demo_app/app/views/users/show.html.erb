<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<p>
  <strong>Microposts:</strong>

    <% @user.microposts.each do |microppost| %>
      <p>
      	<strong>Id:</strong>
        <%= microppost.id %>
      	<strong>Content:</strong>
        <%= microppost.content %>
      </p>
    <% end %>
</p>`
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
